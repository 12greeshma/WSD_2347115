// roomModel.js (example model if you need to define room structure)
class Room {
    constructor(roomNumber, roomType, occupancy) {
      this.roomNumber = roomNumber;
      this.roomType = roomType;
      this.occupancy = occupancy;
    }
  }
  
  module.exports = Room;
  