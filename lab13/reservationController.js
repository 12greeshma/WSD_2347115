// reservationController.js
const pool = require('./dbConfig');

function searchAvailableRooms(checkInDate, checkOutDate, roomType, occupancy) {
  // Logic to search available rooms (same as provided earlier)
}

module.exports = {
  searchAvailableRooms,
};
